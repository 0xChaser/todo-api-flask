services:
  todo-api:
    image: 0xchaser/todo-api:latest
    ports:
      - "80:5000"
    environment:
      - DB_PATH=/app/data/todos.db
    volumes:
      - todo-data:/app/data
    restart: unless-stopped

volumes:
  todo-data:
